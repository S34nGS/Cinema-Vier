# Cinema Project
Hoe run je de code?

## Linux / Macos
voer het volgende bestand uit in je terminal, in de root directory van dit project
```
./run.sh
```

## Windows
voer het volgende bestand uit in je terminal, in de root directory van dit project
```
./run.bat
```